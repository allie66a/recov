# 回本计划计算器 — 环境变量示例
#
# 实际值只配置在 Vercel 环境变量里, 绝不写进本仓库!
# Vercel: Project Settings → Environment Variables → 新增 TUSHARE_TOKEN

# Tushare token (必填)。支持主备双 token, 用逗号分隔: 主线失败自动降级副线
# 主线: 官方 tushare (2100积分起)
# 副线: xiaodefa (15000积分, 支持 VIP 接口) — 可选
TUSHARE_TOKEN=你的主线token,你的副线token(可选)
